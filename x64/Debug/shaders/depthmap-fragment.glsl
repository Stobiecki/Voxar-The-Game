#version 330 core

// in vec4 fragPos;
// out vec4 outColor;

// uniform samplerCube tex;

void main()
{
    // nie produkujemy zadnego koloru
    // vec3  depthGrayscale = texture(tex, fragPos.xyz).rrr;
    // outColor = vec4(depthGrayscale, 1.0f);
}
